# Basic Admin Plugin Usage

Package this template with:

```bash
tar -czf basic-admin-plugin.tar.gz payincus.plugin.json README.md dist templates docs
```

It demonstrates an admin-only settings surface and does not expose a user page.
