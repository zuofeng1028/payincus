# Admin Tool Example

This template shows the smallest PayIncus admin-only plugin.

Package it from this directory:

```bash
tar -czf admin-tool-plugin.tar.gz payincus.plugin.json README.md dist templates docs
```
