# User Sidebar Plugin Usage

Package this template with:

```bash
tar -czf user-sidebar-plugin.tar.gz payincus.plugin.json README.md dist templates docs
```

It demonstrates a user-facing page that must use Public API surfaces instead of admin APIs.
