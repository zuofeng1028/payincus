# Coupon Plugin Usage

Package this template with:

```bash
tar -czf admin-user-mixed-plugin.tar.gz payincus.plugin.json README.md dist templates docs
```

It demonstrates a mixed admin settings page and user coupon page.
